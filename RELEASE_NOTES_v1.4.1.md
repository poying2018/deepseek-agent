# DeepSeek Agent v1.4.1 发布说明 — 不再需要你反复说「继续」

## 修的问题

v1.3.9 之后出现的这个行为：**Agent 跑一会儿就停下来，必须你说一句「继续」它才往下走。**
不是模型变懒了，是我在 v1.3.9 加的「失控轮次看门狗」在掐正常任务。

## 实测根因

把本机 22 个会话、63 个轮次的日志解出来统计了一遍：

| | 中位 | P90 | P99 | 最长 |
|---|---|---|---|---|
| 每轮步数 | 25 | 55 | 239 | 239（正常完成） |
| 每轮 token | 2.98M | 15.6M | 56.2M | 56.2M |

而看门狗的出厂阈值是**第 25 步**。也就是说你一半的轮次都会撞上它注入的那句
「预算已经用完，从现在起不要再调用任何工具」—— 模型照做、当场收尾，剩下的活儿就得你再说一句
「继续」。会话里能看到一条这样的：任务跑到第 25 步时正做到「同一问题在另外三个屏幕也存在，
批量修正」，明显没做完就被打断了。全程统计下来：注入 13 次，硬关轮 2 次。

还有一个更关键的发现：**换成按 token 设阈值也不行**。当初促使我加这个看门狗的那次失控只有
57 步 / 11.2M token，而你的日常一轮就能烧到 15–56M —— 失控的那次比正常干活还便宜。
任何按「步数」或「花费」划的线，切到的都会是正事。

真正能把「在干活」和「在打转」分开的，只有一个结构性信号：**同一个工具调用原封不动地连续重复**。
而它在 63 轮里最长的连续值是 1 次 —— 误伤率为零。

## 修法

看门狗只保留那一个零误伤的判据：

- **步数上限默认关闭**（`maxSteps` 25 → 0）。想要硬上限的人仍可在设置里或
  环境变量 `DSH_RUNAWAY_GUARD_MAX_STEPS` 显式打开，行为与以前一致。
- 保留并调整「原地打转」检测：**同一工具调用连重 4 次**注入收尾提示、**连重 8 次**关掉这一轮
  （原来是 3 / 5）。
- 负数配置现在被夹到 0（= 关闭），而不是「任何一步都算超限」。

代价也说明白：像当初那次「93 个各不相同的调用、每个都在动但整体没进展」的失控，
这一版**不会再自动拦下**了 —— 因为这种形态在可观测信号上和正常重活儿无法区分。
宁可漏拦，不可拦错。真遇到时手动点停止即可，或自己把 `maxSteps` 设成一个你能接受的值。

## 顺带修的两个

- **关于面板不再报错过底座版本**：那里硬编码着 `v0.1.2-rc.1`，而实际内核早就到了 `0.1.5-rc.2`。
  现在改成运行时实读内核包版本（开发态与安装版都验证过取值正确），读不到就整行不显示，
  绝不报一个假版本号。
- **归档的会话又跑回列表里**：这是 v1.4.0 修的那个缺陷（归档管理认不出新版会话日志文件名，
  把每条归档都判成"数据已丢失"并写空归档名单）。如果你是从 v1.3.9 直接升到本版，
  这个修复一并拿到。**已经被清空过的名单没法自动恢复**（那份记录只存一处、没有副本），
  但会话本身一条都没丢，重新归档一次即可，之后不会再被抹掉。

## 维护侧（不影响使用）

补丁落地门禁修了一个机制性问题：本地 `check:patches` 验的是开发克隆的字节、CI 验的是清单
 ref 现取的字节，v1.4.0 就出现过「本地全绿、CI 未命中」。现在 `prepare-bundle` 会把暂存来源
落盘，`check:patches --release-strict` 在来源不是发行输入时拒绝给结论，并且 **CI 里多了一道
真正的把关步骤**：补丁未命中就红，出不了包。另外那条已经失效的 codearts 登录补丁被删掉了
（上游已原生修好，缘由记在补丁表顶部的墓碑注释里）。

## 验证

- `check:guard` 新增 9 条断言，其中直接对应本次反馈的回归是：默认配置下第 25 / 26 / 29 / 100 / 239
  步都**不许**注入、不许关轮，且全程不留一行介入日志；同时连重 4 次仍会劝、8 次仍会关。
- 八项本地检查全绿；发版 preflight 5 步通过。

> ℹ️ 装完请**完全退出再启动**（看门狗是内核侧插件，必须新进程才生效）。
> 从 v1.3.9 升上来的话，本版同时包含 v1.4.0 的启动提速约 2.8 秒、快照改存到工作区、
> 归档状态不再被静默删除。

# DeepSeek Agent v1.0.3

## 🚑 修复 v1.0.2 启动失败（关键热修）
v1.0.2 内置的 0.1.5-rc.1 内核因两个独立问题无法启动，本版彻底修复：

1. **上游回归缺补丁**：0.1.5 起 `dsh-client-connection` 删掉了 `webServer` inject 却仍在使用，任何调用 `ctx.connection.rpc.handle()` 的插件都会让内核在加载 profile 时崩溃。原「内核更新器」升级时会自动打的兼容补丁，在改为随包发布后一度丢失——现已把补丁逻辑搬进构建流程（`prepare-bundle` 新增 [2.6/4] 内核兼容补丁，幂等、上游修复后自动跳过）。
2. **锁步组混版**：`pnpm` 按宽依赖范围把 230 个锁步包里的 26 个留在了 0.1.2-rc.1（如 `dsh-llm` 缺少 `dsh-subagent@0.1.5` 需要的导出），同样导致启动即崩。现以 `pnpm.overrides` 把 **整套 `@deepseek-ai/dsh-*` 锁步组统一钉到 `0.1.5-rc.2`**（230 个包全部同版本，一致性已验证）。

## 内核底座
- 内核整组升级到 **`0.1.5-rc.2`**（上游 `next` 通道；`dsh` + `dsh-web-app` + 全部锁步包同版本）。

## 其他（继承自本版累积改动）
- 检查更新面板新增「当前应用」详情区（应用版本/内核底座/运行模式/系统平台/运行时/安装位置/数据目录）。
- 修复 API 限流走 atom 兜底时误报「这一版没有填写更新说明」——现在兜底路径也能提取完整发布说明。
- `pnpm update-core` 脚本升级：升内核时自动同步 overrides 锁步钉子，杜绝再次混版。

## 说明
- `dsh-codex-timeline` 维持停用（作者仅声明支持 0.1.2-alpha.3），待其适配后自动恢复。

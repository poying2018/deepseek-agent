# DeepSeek Agent v1.4.4 发布说明 — 正式版与纯净版终于各查各的轨

## 这版修的是什么

你有两条交付轨：**正式版**（`main` 分支，带 28 个插件，tag `v1.4.3`）和**纯净版**
（`vanilla` 分支，官方裸底，tag `v1.4.3-vanilla.1`，在 GitHub 上是 prerelease）。
它们共用同一个 appId、同一个产品名、同一套安装包命名规则 —— 从包上**看不出彼此**。

而更新器此前只查 `/releases/latest`，atom 兜底里再按"是不是正式版号"过滤。
GitHub 那个 `/latest` 端点**结构上永不返回 prerelease**，于是：

- 纯净版用户点一次「检查更新」，拿到的必然是正式版的包 —— 一个 220 MB 的全插件安装器；
- 反过来正式版用户看不到纯净版，这个方向碰巧是对的，但靠的是"prerelease 被 latest 排除"
  这个副作用，不是设计。

## 改了什么

新增 `src/main/update-tracks.js`（纯函数，不引 electron，所以能在普通 node 下用假 payload
断言），钉三条不变量：

1. **装在哪条轨由自身版本号决定**：`-vanilla.N` 后缀算纯净轨；解析不出来（自编译等）
   一律按正式轨 —— 宁可漏提示纯净版更新，也不把全插件包误推给纯净版用户。
2. **查询按轨道分**：正式轨仍走 `/releases/latest`（一次请求，配额没翻倍）；纯净轨改走
   `/releases` 列表挑本轨最新。atom 兜底同一条规则。
3. **跨轨不是升级**：`1.4.3-vanilla.1` 按 semver 比 `1.4.3` 是**降级**，所以跨轨一律不比较
   版本号，直接返回 `cross-track` 且**不携带安装包**。

面板顶部多了「更新轨道」开关，默认且唯一自动选中的是本机那条轨。选了另一条轨时，
动作区没有下载、没有安装，只有一段说明加一颗**贴着说明**的「打开系统卸载入口」
（Windows 打开「设置 → 应用 → 应用和功能」，macOS 打开「应用程序」文件夹，打不开时
只给文案不报错）。卸载这一步由你在系统界面里自己点，应用不代做不可逆动作。

## 顺手修掉的一个老 bug

核对截图时发现面板主按钮是**白底白字**：这主题里 `--dsw-alias-brand-primary` 实测
= `#f9fafb`（近白），而 `--dsw-alias-on-brand` **根本没定义** → 回退 `#fff`，
背景和前景的亮度差只有 5。也就是说「下载更新 / 立即安装」从 1.4.x 起在裸底主题下
就一直糊成一片，只是从来没人截过这张图。新加的轨道胶囊中招同一个原因。
两处都改成 `label-primary × bg-layer-2` 的反色对：深色主题白底黑字、浅色主题自动翻成
黑底白字，不依赖任何皮肤补 token。实测亮度差 5 → **206**。

## 验证

- 新增 `pnpm check:updater`（67 条断言），并入 `release:preflight` 第 6 步。假 payload 覆盖
  轨道判定全表、分轨挑选、跨轨不返回 asset、降级不报可更新、rc 两边都不推、
  产品前缀不匹配的资产一律不认（历史上真踩过 `LJANX-9.14.23-*.dmg` 被当成自己的更新包）。
- **现网真值**：拿 `api.github.com` 对五种组合实跑 —— 正式装 1.4.3 报「已是最新」；
  纯净装 1.4.3-vanilla.1 报的本轨最新就是 `1.4.3-vanilla.1`（不再是正式版）；
  1.4.2 报可更新且资产名 `DeepSeek-Agent-1.4.3-Windows-Setup.exe`、体积 222,117,879；
  两个方向的跨轨都返回 `cross-track` + `asset: null`。
- **真渲染截图**：主线副本与纯净版副本各起一个实例（独立 user-data-dir，不碰已装版本），
  核对轨道开关、跨轨警告块、按钮可读性与"不滚动就能看到主动作"。
- 一个诚实的缺口：atom 兜底分支只有假 payload 断言。本机到 `github.com` 网站域名当前不通
  （`api.github.com` 通），那条路**无法**现网验证，代码注释里也写明了这一点。

## 升级方式

左下角「检查更新」→ 下载 → 立即安装即可，正常覆盖安装。

**只有跨轨道**（正式版 ↔ 纯净版）需要你先卸载当前版本再装另一条轨的包 ——
应用内会直接把你引导到系统的卸载界面。两条轨共用数据目录，换轨不会丢工作区，
但你在应用内自己装过的第三方插件会跟着穿过这条边界（纯净版保证的是"出厂不带"，
不是"替你删"）。

## 纯净版侧的对应变化（`vanilla` 分支 `v1.4.4-vanilla.1`）

纯净版从这版起**保留一个仓内插件：更新面板 `dsh-update-check`**。理由是机制性的而不是
偏好性的：GitHub 的 `/releases/latest` 不返回 prerelease，没有这块面板，纯净版用户
靠自己查不到本轨道有没有新版。留下的是副作用最小的那个 —— 宿主半边 `inject` 为空
（不占端口、不读文件、不改路由），客户端半边只注册一个侧栏按钮，不参与推理也不改交互，
所以不会污染"这毛病是不是插件叠出来的"这个纯净版要回答的问题。
真正改行为的（看门狗、输入框兜底、皮肤）依旧一个都不带；`check:profile` 的第 0 组
不变量相应改成"内置清单必须恰好这一项，社区/外观两张清单仍须为空"。

内核仍是 **0.1.5-rc.2**（npm 上的 `latest`），两条轨同一内核。

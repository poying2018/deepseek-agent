# 纯净版（vanilla 分支）插件清单：**刻意留空**。
#
# 本分支不打包任何外部插件，仓内也只留更新面板一个（见 src/main/own-plugins.js
# 文件头的理由），profile 的 bundles 只剩内核本体两行
# （@deepseek-ai/dsh-base、@deepseek-ai/dsh-web-app）。留空而不是删文件，是因为
# prepare-bundle.js 与 CI 都以这个文件为输入契约。
#
# 主线（main 分支）上的完整清单与每条的来源/钉版本理由都在 git 历史里：
#   git show main:plugins.manifest.yaml
plugins: []

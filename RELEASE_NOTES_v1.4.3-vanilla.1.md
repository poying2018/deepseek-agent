# DeepSeek Agent v1.4.3-vanilla.1 —— 纯净版（官方裸底，无插件）

这是 **`vanilla` 分支**的产物：**只装官方内核本体，一个插件都不带**。
它不是主线的新版本，而是一把用来做判断的尺子——某个行为问题究竟是插件/皮肤叠出来的，
还是上游内核本身就有。主线（带 28 个插件）仍然是 `main` 分支与不带后缀的版本号。

内核版本 **0.1.5-rc.2**，也就是 npm 上的 `latest`（官方另有 `next: 0.1.5-rc.3`、
`alpha: 0.1.7-alpha.1`，本包按你的选择**不升轨**，保持与主线同一内核，这样对比只有一个变量）。

## 装了什么

只有两行 bundle：`@deepseek-ai/dsh-base` 与 `@deepseek-ai/dsh-web-app`。

**刻意保留**（不引入功能，只修缺陷或纯性能）：

- 三处内核包补丁：设置页侧栏在插件多时无法滚动、未鉴权第三方模型不再出现在选择器、
  `dsh-client-modules` 启动提速（combo/source-map 构建的纯冗余 CPU，输出逐字节等价）
- 外壳侧启动与 GPU 优化：GPU 光栅化、反节流开关、关掉 Windows 遮挡计算与 MediaRouter、
  补丁戳记快路径、CSS 每文档只注入一次
- 两处宿主适配：目录选择器的 browse 两面（否则在 Electron-as-Node 宿主里选不了工作区）、
  关闭 client HMR（否则多标签会耗尽 SSE 连接）

**刻意去掉**：全部 28 个插件，以及为插件服务的注入层——包括 cordis 托管区里的
`repeat-tool-reminder` 能力行和 preload 的输入框点击兜底。理由很直接：这两者本身就在
改交互行为，留在裸底里会**污染**"这问题是不是插件引起的"这个结论。

## 实测数字（同一台机器、同一判据 TCP ready、内核数据副本各跑 3 次）

| 形态 | 内核启动 |
|---|---|
| 主线改补丁前（28 插件，上游原版） | 7.3 / 7.3 / 7.6s |
| 主线现状（28 插件 + 启动提速） | 4.8 / 5.1 / 5.5s |
| **本纯净版（0 插件）** | **3.4 / 3.6s** |

也就是说插件层大约贡献了 **1.5 秒**启动（主要来自每个插件的前端 bundle 都要被拼进
combo 脚本与 source map）。界面完整性也确认过：侧栏、新会话、工作区、设置、hero 输入区
都在（截图核对），不是"能启动但白屏"。

## 顺带修掉一个会黑屏的隐患（主线同样受影响）

做这个分支时必须把 profile 里的插件行清掉，结果撞出真问题：自愈逻辑对
`dependencies` 里列过的包是**无条件保留**的。一旦某个包记在 dependencies 里但实际没装
（换安装包时链接失效、插件安装中断、或发行版改版不再带它），那一行会留下，而内核解析不到
任何一个 bundle 就会**中止整个 web 壳启动**：

```
Error: dsh: cannot resolve profile bundle "@ace-zone/dsh-market" …
```

现在改成：记在 dependencies 但解析不到的，剔除该行并留一行日志；`dependencies` 声明本身
**不动**（那是用户的安装意图，归插件面板管）。这条修复由 `pnpm check:profile` 新增的
断言钉住（含"用户真装着的插件绝不能被误删"这条反向保护）。

## 数据与回退

- 首次启动会**自动清理**老 profile 里指向已消失插件的 bundles 行与死链，不会黑屏；
  你的工作区、会话、模型授权数据都不动。
- 你在应用内**自己装过且确实还装着**的插件会被保留（这是刻意的，不能擅自删用户装的东西）。
  想要绝对干净的对比，请用一个**新的数据目录**启动。
- 本包**没有**应用内检查更新（那是插件）。要回到主线：直接安装 `main` 分支对应的
  正式版本安装包即可，数据同源、无需迁移。

## 验证

- 八项本地门禁全绿（`check:guard`/`check:archive` 随插件一并移除；
  `check:profile` 扩到 33 组断言，含本分支不变量：三张插件清单为空、manifest 无声明、
  仓内插件目录不存在）。
- 端到端：在数据副本上跑宿主自愈 → bundles 收敛到核心两行 → 内核 READY 3.4s →
  起真实外壳截图核对界面。
- 本分支产物一律标 `--prerelease`，不会抢走主线 release 的 Latest 标记。

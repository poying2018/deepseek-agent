# DeepSeek Agent v1.0.2

## 内核底座升级
- **内核从 `0.1.2-rc.1` 整组升级到 `0.1.5-rc.1`**（`@deepseek-ai/dsh` + `dsh-web-app` + 全部锁步包，800 依赖重新解析）。
- 内核随安装包整体发布（单轨更新），更新应用本体即完成内核升级。

## 兼容性处理
- 构建期新增**内核兼容性门禁**：`compatibility.json` 声明未覆盖当前内核版本的内置插件将自动停用（可逆），不拖累整体可用性。
  - 本次停用：**`dsh-codex-timeline`**（仅声明验证到 `0.1.2-alpha.3`，其搜索接口在新内核上不可用；待作者更新声明后自动恢复）。
- 全部插件 peer 依赖与宿主锁定版本核对通过；启动判定检查通过。

## 其他改进
- 检查更新面板新增**「当前应用」详情区**：应用版本 / 内核底座版本 / 运行模式（标准安装/便携）/ 系统平台 / Electron·Node·Chromium 运行时 / 安装位置 / 数据目录。

## 说明
- 内核版本来源：npm `@deepseek-ai/dsh` 的 `latest` 标签（`0.1.5-rc.1`）。
- 安装包由 CI 在 `v1.0.2` tag 上自动构建并发布。

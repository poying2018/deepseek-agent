# DeepSeek Agent v1.4.2 发布说明 — 删掉看门狗，任务不再被任何阈值打断

## 改了什么

**自研的失控看门狗 `dsh-runaway-guard` 整体删除。** 现在没有任何东西能结束你的回合：
任务会一路跑到底，不需要再说「继续」。

替换它的是上游自带的一个**只提示、不打断**的机制
（`@deepseek-ai/dsh-repeat-tool-reminder`）：当同一个工具用**完全相同的参数**连续调用到
3 / 5 / 8 次时，往上下文里注入一条提示（点名工具、连重次数、参数摘要），叫模型换做法或者
按已有证据收尾 —— 它**永远不结束回合**，真人一说话计数就清零。

之所以只留这一个判据：实测下来它是唯一零误伤的（见下）。

## 顺带把「无限循环到底是什么」查清了

把本机 22 个会话、63 个轮次、2682 个工具结果全部解开统计，先说结论里最重要的两句：

**内核的续跑判据只有一条 —— 上一条模型输出里还有没有 tool-call。没有进展判定、没有预算、
没有「该停了」的概念。** 所以只要模型每句都点一个工具，循环就一直继续。这解释了为什么
"停不下来"是可能的，也解释了为什么我上一版按步数设的闸门是错的东西。

**而你真正那次失控，根本不发生在「步与步之间」，它发生在一条消息内部。** 现场是那次
58 步 / 93 个调用 / 19.5 分钟的轮次（最后你手动停的）：第 58 步的**单条思考链有 101,591 字、
8,383 行，其中只有 468 行是唯一的**，末尾是 `Writing. / Go. / Now. / Final. / Output. / OK.`
各重复约 10 次；紧接着下一轮的第 7 步到 163,777 字（`Go.` 出现 4873 次、
`Let me output.` 2425 次），两步都是在流式输出中途被掐断的。93 个调用只产生了 1 次写操作。

也就是说：**模型的思考退化成了一个 6 token 的循环，而这一步因此永远不会结束**。
任何"第 N 步"级别的闸门都不可能看见它 —— 计数根本还没走到下一步。

被数字排除掉的其它猜测（都不是成因，别再往这些方向找）：

| 猜测 | 判定 | 支撑数字 |
|---|---|---|
| 工具报错后反复重试 | 不成立 | 失败结果 32/2682 = 1.2%，最坏轮 3/65 |
| 参数被截断导致调用无效 | 不成立 | `arguments` JSON 解析失败 **0/2666** |
| 对同一个文件反复操作 | 不成立 | 调用目标多样性 0.56–0.94，callId 重复 0 |
| 上下文压缩抖动 | 不成立 | 全库 `compaction/start` 只有 1 次且失败 |
| 正文空话连篇 | 不成立（但**思考链里成立**） | 可见文本 ≤12 字的仅 8/1778；而 22.3% 的步在 reasoning 里有催促式短行 |

同时也说明了为什么删掉阈值而不是"调高一点"：正常轮次步数**中位数就是 25**、P99 **239**，
每轮 token 中位 2.98M、P99 56.2M，而当初那次被当成"失控"的只有 57 步 / 11.2M
—— **失控那次比你日常干活还便宜**。按步数或花费划线，切到的必然是正事。

**代价我不藏：现在没有任何机制能拦住上面那种思考链退化。** 真要做对，需要的信号是
「单条思考链的长度 / 重复率超限时只掐这一次生成并重来该步」（而不是掐整轮）、
或「连续 N 步零副作用」、或一个可机器校验的完成断言 —— 这几条都在内核侧，不在发行版能改的范围内。
真遇到时按停止键仍然有效。

## 还修了一个会把 App 变砖的隐患

启用上游那个提示包时踩到一条硬约束：**`bundles` 清单里的每个包都必须在自己的
`package.json` 声明 `dsh.bundle`**，否则内核直接抛
`profile bundle "…" declares no dsh.bundle` 并**中止整个 web 壳启动**（黑屏，实测在副本上
120 秒起不来）。这类内核自带的 host 能力包只能走 `cordis.patch.yml` 的托管区插入行 ——
上游的 `dsh-web` 本来也是这么处理的，现在这个提示包也一样。

顺手把这一类"必炸形态"变成自愈：启动前会剔除 `bundles` 里**能读到包定义却没声明
`dsh.bundle`** 的行；读不到包定义的行一律保守保留（profile 的解析链接是内核启动时才修复的，
此刻看不到不等于坏）。另外新增的门禁 `check:profile` 也钉住了另一件事：**删除内置插件不会
把老用户的 App 变砖**（残留的 bundle 行会被自动清掉并删除死链），同时你在应用内自装的插件
绝不会被误删。

## 验证

- 端到端：在数据副本上先复现黑屏 → 自愈剔除该行 → 内核 8.9 秒 READY →
  内核自己的 `--dump-config` 里能看到这条能力 entry（确实装载，而不是被忽略）。
- `check:profile` 28 组断言：含上游 reminder 的**契约测试**（只注册 `tools/post-execute`
  这种不能掐轮的口子；连重 1/2 次不注入；3 次注入 notice；回合结局仍是 `next()` 的 pass；
  别人给的 block 原样透传）。
- 八项本地检查全绿，发版 preflight 5 步通过。
- 看门狗自身已从内置清单移除，`builtin-plugins/` 下目录与 `check:guard` 一并删除。

> ℹ️ 装完请**完全退出再启动**（profile 的 bundles 与托管区补丁是启动时重写的，
> 残留的 `dsh-runaway-guard` 行会在第一次启动时被自动清理）。
> 从 v1.3.9 升上来的话，还一并包含：启动提速约 2.8 秒、快照改存到工作区、
> 归档状态不再被静默删除、关于面板显示真实底座版本。

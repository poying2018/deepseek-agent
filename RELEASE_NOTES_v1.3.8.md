# DeepSeek Agent v1.3.8 发布说明 — 打开右侧边栏不再压住会话头部按钮

## 修的问题

打开右侧边栏（文件 / 任务管理）后，会话头部那一排按钮会互相重叠，最右边的按钮被面板盖掉一半。
插件装得越多越明显 —— 撤销 / 恢复 / 快照 / 对话撤回 这几个按钮一上来就挤爆了。

## 实测根因

承载 `conversation.session.header.actions` 槽位的三个行容器（内核
`dsh-client-ui-conversation` 的 `_heroWorkspaceRow` / `_workspaceRow` / `_titleRow`）都是
`display:flex` + 默认 `flex-wrap:nowrap` + 不裁剪 overflow。而右侧边栏打开时是**浮层盖在中间列上**
（不是挤压中间列宽度），所以头部这一排一旦超过中间列宽度，就横向溢出到浮层底下 ——
看上去就是"按钮叠在一起"。

在运行中的实例里量过：中间列 386px、行容器 384px，造拥挤态时内容 470px，
最右按钮伸到列外 **84px**。

## 修法

给这三个行容器加 `flex-wrap: wrap` + `row-gap: 4px` + `min-width: 0`（含子项），
让按钮**换行**而不是横向溢出。代价是那一排在拥挤时变成两行（行高 28 → 56）。

规则注入在宿主层（`setupConversationHeaderWrap`，沿用既有的每文档只注入一次的 CSS 注入器），
不打补丁给任何插件、也不改内核包 —— 这一排的宽度取决于装了几个插件，宿主兜底对任意组合都成立。

## 验证

- 在真实实例页面上做对照：拥挤态下 `nowrap` 溢出 84px；换上本次要发布的 CSS 原文后内容收回容器内、
  最右元素落在列边界内 35px、行高变两行，重叠消失。
- 在开发实例的 Electron 窗口里确认注入真的生效：该行计算样式为 `flex-wrap: wrap` + `row-gap: 4px`
  （`row-gap` 是内核规则里没有的指纹，只可能来自本次注入）。
- 插件 peer 检查、目录选择器组合检查、启动判定检查三项门禁通过。

> ℹ️ 装完建议完全退出再启动。另外本版一并包含 v1.3.7 的 CodeArts 自愈重试与
> v1.3.5~v1.3.6 的侧栏滚动、输入框焦点、模型可见性等修复（如果你从 v1.3.4 升级）。

# DeepSeek Agent v1.4.4-vanilla.1 —— 纯净版：更新轨道与正式版分开了

这是 **`vanilla` 分支**的产物：**官方内核本体 + 一个更新面板插件**，其余插件一个都不带。
它不是主线的新版本，而是一把做判断用的尺子 —— 某个行为问题究竟是插件/皮肤叠出来的，
还是上游内核本身就有。主线（28 个插件）在 `main` 分支与不带后缀的版本号。

内核仍是 **0.1.5-rc.2**（npm 上的 `latest`），与主线同一内核，这样对比只有一个变量。

## 这版最重要的变化：检查更新不再串台

纯净版在 GitHub 上是 prerelease（tag `v1.4.4-vanilla.1`），而 `/releases/latest` 这个端点
**结构上永不返回 prerelease** —— 所以此前纯净版用户点一次「检查更新」，拿到的必然是
正式版那个 220 MB 的全插件包。现在：

- 装在哪条轨由自身版本号决定（`-vanilla.N` 后缀 = 纯净轨），查询走 `/releases` 列表，
  只在**本轨**里挑最新；
- 面板顶部有「更新轨道」开关，默认且唯一自动选中的是本机那条轨；
- 切到另一条轨时**没有下载、没有安装按钮** —— 跨轨不是升级（`1.4.3-vanilla.1` 按 semver
  比 `1.4.3` 还是降级），面板只给你一段说明和一颗贴着说明的「打开系统卸载入口」，
  卸载这一步由你在系统界面里自己点。

现网真值核对：纯净装 `1.4.3-vanilla.1` 报的本轨最新就是 `1.4.3-vanilla.1`（不再是正式版），
两个方向的跨轨都返回 `cross-track` 且 `asset: null`。

## 纯净版为什么现在带一个插件

从这版起保留 **`dsh-update-check`（更新面板）**，理由是机制性的而不是偏好性的：
GitHub 的 `/releases/latest` 不返回 prerelease，没有这块面板，纯净版用户靠自己查不到
本轨道有没有新版，只能人工盯发布页。

留下的是副作用最小的那一个：宿主半边 `inject` 为空（不占端口、不读文件、不改
webServer 路由），客户端半边只往侧栏底栏注册一个按钮，所有动作走 preload 暴露的主进程
IPC。它不参与内核推理、不改交互行为，所以不会污染"这个毛病是不是插件叠出来的"这个
本分支要回答的问题。真正会改行为的（看门狗、输入框点击兜底、皮肤）依旧一个都不带，
`check:profile` 的第 0 组不变量也从"清单为空"改成"必须恰好这一项，社区/外观两张清单
仍须为空"。

## 顺带修掉的一个老 bug

核对截图时发现面板主按钮是**白底白字**：这主题里 `--dsw-alias-brand-primary` 实测
= `#f9fafb`（近白），而 `--dsw-alias-on-brand` 根本没定义 → 回退 `#fff`，背景与前景亮度差
只有 5。「下载更新 / 立即安装」从 1.4.x 起在裸底主题下就一直糊成一片。新加的轨道胶囊
中招同一原因。两处都改成 `label-primary × bg-layer-2` 的反色对，深浅主题自适应，
实测亮度差 5 → **206**。

## 保留 / 去掉的清单（与上一版一致）

**保留**：三处内核包补丁（设置页侧栏滚动、未鉴权模型隐藏、`dsh-client-modules` 启动提速
—— 输出逐字节等价，由 `pnpm check:kernelspeed` 钉住）、外壳侧启动与 GPU 优化、
目录选择器的 browse 两面、关闭 client HMR。

**去掉**：其余全部插件，以及为插件服务的注入层（cordis 托管区里的 repeat-tool-reminder
能力行、preload 的输入框点击兜底）。

## 升级 / 换轨须知

- 从**纯净版 1.4.3-vanilla.1** 升上来：正常「检查更新 → 下载 → 立即安装」即可。
- 从**正式版**换到纯净版（或反向）：**先卸载当前版本**再装。两条轨共用数据目录，
  换轨不会丢工作区；但你在应用内自己装过的第三方插件会跟着穿过这条边界 ——
  纯净版保证的是"出厂不带"，不是"替你删"。
- 从 **1.4.2 及更早**升上来：那些版本没有分轨逻辑，装了纯净版后仍可能被推正式版的包，
  升级前请确认「当前应用 → 更新轨道」显示的是纯净版。

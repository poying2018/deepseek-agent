# DeepSeek Agent v1.4.0 发布说明 — 启动更快，归档不再丢

本版四件事：内核启动提速约 2.8 秒；对话快照改存到你自己选的工作区里；
修掉「设置页里归档的对话不显示」（以及它背后更严重的静默删除）；CodeArts 登录不再开两个浏览器。

## 1. 启动过渡页少了约 2.8 秒

主进程要等内核 HTTP 就绪才会离开启动占位页，所以这段时间是看得见的。
用 `--cpu-prof` 采了一遍内核启动，48.7% 的 CPU 花在**一个包**上：
`@deepseek-ai/dsh-client-modules` 每次启动都要把全部插件的前端代码拼成 combo 脚本
和 source map，而且每个 bundle 被处理两遍（批量一遍、单件一遍），没有磁盘缓存。

浪费集中在两处写法上：数换行用 `for...of` 逐码点迭代（在几 MB 的包上自耗 21% CPU），
以及为每一行各建一个单元素数组再 `join`。两处都换成等价写法：
`indexOf` 原生扫描、`"AAAA" + ";AACA".repeat(n - 1)`。

本机严格 A/B（同一份数据副本、同一就绪判据、各跑 3 次）：

| | 第 1 次 | 第 2 次 | 第 3 次 |
|---|---|---|---|
| 改前 | 7.6s | 7.3s | 7.3s |
| 改后 | 4.5s | 4.8s | 4.8s |

产出的 combo 脚本与 source map **逐字节不变**（不是"看起来一样"，是拿上游原实现作真值、
对 78 个真实 bundle 逐一比对出来的），所以这是一次纯性能改动，没有任何行为变化。

> ℹ️ 顺带记一个已证伪的方向：`NODE_COMPILE_CACHE`（磁盘级 V8 编译缓存）对内核启动没有收益
> —— 热缓存 8.1s vs 基线 7.6s。瓶颈从来不是模块编译。

## 2. 对话快照存到你选的工作区里

以前快照存在 `%APPDATA%\ljanx\dsh-data\undo-snapshots`，跟你实际干活的目录没关系，
换机器/挪项目时快照就跟不上。现在默认落在**最近使用的那个工作区**下的
`<工作区>\.dsh-undo\snapshots`，随项目走、可以直接跟着仓库一起备份。

已有的快照会自动搬一次：复制 → 数一遍文件数和字节数校验 → 确认无误才把旧目录改名为
`undo-snapshots.migrated-<时间戳>` 留着（不删）。校验没过就回退成原路径、什么都不动。
想钉在某个固定位置，设环境变量 `DSH_UNDO_ROOT` 即可，设了就不再自动跟工作区走。

> ⚠️ `.dsh-undo` 现在会出现在工作区目录里。建议加进 `.gitignore`。

## 3. 设置页里归档的对话不再"消失"

打开设置页的归档卡片，归档列表是空的 —— 而且更糟的是：**这个动作本身会删掉你的归档状态**。

根因是归档管理插件（`@mlgbnb/dsh-archive-manager`）认不出新版的会话日志文件名
（内核已改名成 `session.v3.jsonl.zstd`），于是把每条归档都判成"数据文件已不存在的幽灵"，
清掉并**把空的归档名单写回 `workspace.json`**。也就是说，每开一次归档页，归档记录就被抹一次。

修法是给这个插件补 7 处兼容补丁：认新文件名（含 zstd 压缩内容能正常读出正文）、
投影缓存的两种存放形态都读（目录式 + 聚合表）、只对真正查无此会话的行做幽灵清理、
且不再回写 `workspace.json`。上游 1.0.10 这三个缺陷都还在（7 个锚点只命中 4 个），
所以清单里钉在 1.0.7，升版前会先跑一遍归档专项检查。

如果你的归档列表在这之前已经被清空过：**那份名单没法自动恢复**（它只存在
`workspace.json` 的 `archivedSessionIds` 一个地方，被写空之后没有第二份记录，本机也没有留下
改名前的副本 —— 已核实）。好消息是**会话本身一条都没丢**，22 个会话目录与日志都在，
只是"已归档"这个标记需要你在列表里手动重新归档一次。本版之后它不会再被删。

## 4. CodeArts 登录只开一个浏览器

v1.3.x 期间上游改了两次登录流程，把"登录只开一个浏览器"这条兼容补丁的锚点冲掉过两回，
最近一次是**静默**的：日志里只有一行 ⚠️，包照出、功能照坏，表现为点登录后
系统浏览器和内置窗口各开一个。

**更正（同日补记）**：本版发布时这里写的"锚点已对着当前上游重新钉住"不对。CI 的日志显示
那条补丁在真正的构建输入上**没有命中**（`⚠️ 插件补丁未命中 dsh-codearts-auth/lib/jet-hub-rpc.js`），
而本地门禁却给了绿灯 —— 因为本地暂存用的是开发克隆，CI 用的是清单 `ref: master` 当时的字节，
两边不是同一份。对着上游 master 复核后的结论是好消息：**这件事上游已经自己修好了**
（CodeArts 分支改成两步式 `codearts.startLogin({ refName })`，宿主侧不再开浏览器，
登录页只由客户端 `window.open` 开一次，再被我们转成系统浏览器）。所以本版的登录行为是正确的，
那条补丁则属于死锚，已删除（并在补丁表顶部留了墓碑，写清它失效过三次、以及什么情况下需要重新评估）。

至于本地为什么会假绿，加了三道结构性修复，从下一版起生效：

- `prepare-bundle` 会把每个插件的**暂存来源**写进 `bundle-runtime/staging-provenance.json`
  （开发克隆会带上它的 commit）；
- `pnpm check:patches --release-strict` 遇到来源里有开发克隆就拒绝给出"已落地"的结论
  （本机没发言权时它只如实说没发言权，不冒充绿灯）；
- **CI 里新增同名的一步**：它跑在真实发行输入上，补丁未命中就直接红，出不了包。
  红掉时的处置是重新锚定、或像这次一样确认上游已修好后删掉那条。

第 1 条那处性能改写也补了一道独立门禁 `pnpm check:kernelspeed`（preflight 第 5 步）：
`check:patches` 只断言补丁在位，断言不了"改写仍等价"，而这条补丁的全部价值恰恰是等价。

## 验证

- 内核启动：本机同一数据副本 A/B 各 3 次，见上表；combo 与 sourceMappingURL 逐字节一致。
- 八项本地检查全绿：插件 peer、目录选择器组合、轮次闸门、快照存储根、归档管理、
  补丁落地、启动恒等、启动判定；插件状态扫描无阻塞项（唯一提示是 `dsh-workspace-path`
  的本地克隆落后 2 个提交，而清单钉的是固定 commit，不影响发行输入）。
- 发版 preflight 5 步通过。

> ℹ️ 装完建议完全退出再启动（快照迁移与归档补丁都要在新进程里才生效）。
> 本版一并包含 v1.3.9 的失控轮次自动止损、v1.3.8 的侧栏压住头部按钮修复
> （如果你从更早版本升级）。

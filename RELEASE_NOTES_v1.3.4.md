# DeepSeek Agent v1.3.4 发布说明 — 侧栏底栏排版修复 + 移除内置 Qoder provider

本版是**修排版和收边界**：没有新功能，主要是把侧栏左下角那一排挤成一团的问题彻底解决，
并把一个不该由发行版默认启用的第三方 provider 撤下来。

##  修复：侧栏底栏「设置」被图标压住

底栏原本是**一行**：`⚙ 设置` 加四个功能图标（重启 / 检查更新 / 手机遥控 / 工作区）。
在展开态这一行可用宽度只有 207px，而四个 36px 图标固定占 144px，`⚙ 设置` 按钮自然宽约 77px
—— 加起来需要 221px，**差 14px**。被压缩的是设置区（它允许收缩），于是「设置」两个字直接
溢出到图标底下；如果窗口再窄一点，连版本号胶囊的尾字符都会被裁掉。

更麻烦的是：把底栏压成一行的改造**不止一个插件在做** —— `dsh-web-restart`、`dsh-mobile-plus`、
`dsh-update-check` 各自都写了一套同样的规则，只要有一条命中就会塌成一行。本版把三处全部停用，
底栏恢复官方默认的**上下两行**：上行四个图标，下行 `⚙ 设置`，文字完整可见。
代价是侧栏底部高约 36px。

同时底栏不再注入版本号小胶囊 —— 在两行布局下它其实放得下，但那一排的定位是"功能入口"，
版本号属于状态信息；版本仍然可以在**鼠标悬停「设置」**时看到（`设置 · DeepSeek Agent v…`），
插件面板里也有完整的版本矩阵。

> ⚠️ 装完本版需要**完全退出后重新启动**才看得到效果：插件的客户端模块是启动时一次性读入的，
> 页面刷新（Ctrl+R）不会重新加载它们。

## 📦 变更：`dsh-provider-qoder` 不再随包分发

上一版起内置的 Qoder provider（作者 mo-n 的独立仓库）从发行版移除，不再默认启用。
原因：它接的是 Qoder 自己账号体系的适配层，我们不维护它，却以"内置 + 默认启用"的形式
替它背书 —— 不用 Qoder 的用户也会多看到一个 provider 入口和设置页，出问题却回到发行版这里。

需要它的用户仍可在应用内从 npm 自行安装 `dsh-provider-qoder`，功能不受影响；
**已经装过的安装不会被动删除**（升级后它照样留在你的插件列表里）。

内置插件数：29 → 28。

## 🔧 内部修复：兼容补丁执行器支持同一插件多条补丁

`applyPluginRuntimePatches` 原先用 `find` 取补丁条目，同一个插件写第二条会**静默不生效**
（不报错、日志也没有）。改成逐条应用。本版正好给 `dsh-plugin-dashboard` 加了第二条补丁，
顺带把 tooltip 里残留的旧品牌字样 `JackDSH` 换成对外品牌名 `DeepSeek Agent`
（只是显示文案；宿主侧为了兼容上游插件仍双写 `LJANX_*` / `JACKDSH_*` 环境变量，未改动）。

## 验证

- 打包链路 `prepare-bundle` 两种来源（本地源码 / CI 同路径的 `--source public`）均通过；
  暂存区 28 个插件，无 `dsh-provider-qoder` 残留。
- 插件 peer 依赖检查、目录选择器组合检查、启动判定检查三项门禁全部通过。
- 底栏修复在真实窗口上截图确认：恢复上下两行、「设置」完整可见。
